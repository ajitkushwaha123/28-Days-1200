#include<bits/stdc++.h>
using namespace std;
int main()

{
    int n,q;
    cin>>n>>q;

    vector<int> v;

    for(int i=0; i<n ; i++)

    {
        int ele;
        cin>>ele;
        
        v.push_back(ele);
    }

    while(q--)
    {
        int s;
        cin>>s;

        if(s == 1)
        {

        }

        else if(s == 2)
        {
            
        }
    }
}